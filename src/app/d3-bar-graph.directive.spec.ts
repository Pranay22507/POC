import { D3BarGraphDirective } from './d3-bar-graph.directive';

describe('D3BarGraphDirective', () => {
  it('should create an instance', () => {
    const directive = new D3BarGraphDirective();
    expect(directive).toBeTruthy();
  });
});
