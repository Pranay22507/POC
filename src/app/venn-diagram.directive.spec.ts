import { VennDiagramDirective } from './venn-diagram.directive';

describe('VennDiagramDirective', () => {
  it('should create an instance', () => {
    const directive = new VennDiagramDirective();
    expect(directive).toBeTruthy();
  });
});
